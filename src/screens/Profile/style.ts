import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  profileScreenContainer: {    
    paddingTop: 12,
  },
  tabsContainer: {
    height: 350,
    marginTop: 15,
  },
});

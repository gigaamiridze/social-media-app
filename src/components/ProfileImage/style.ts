import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
  userImageWrapper: {
    borderWidth: 1,
    borderStyle: 'solid',
    borderColor: '#F35BAC',
    borderRadius: 50,
    padding: 3,
  },
});